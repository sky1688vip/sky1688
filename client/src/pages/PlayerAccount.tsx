import { ArrowRight, CircleCheck, Link2, LockKeyhole, UserRound } from "lucide-react";
import { startLogin } from "@/const";
import { PublicShell } from "@/components/PublicShell";
import { QueryError, QueryLoading } from "@/components/QueryState";
import { Button } from "@/components/ui/button";
import { humanizeError } from "@/lib/admin";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";

export default function PlayerAccount() {
  const { user, loading } = useAuth();
  const canLoadProfile = !loading && user?.role === "user";
  const profile = trpc.accounts.player.me.useQuery(undefined, { enabled: canLoadProfile });
  return <PublicShell><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 sm:py-16"><div className="max-w-3xl"><p className="eyebrow">PLAYER ACCOUNT</p><h1 className="mt-4 font-display text-4xl font-black tracking-tight text-white sm:text-6xl">သင့် SKY1688<br /><span className="text-lime-200">Player account</span></h1><p className="mt-5 max-w-2xl text-base leading-7 text-emerald-50/65 sm:text-lg">Player account ကို Agent invitation link ဖြင့်သာဖွင့်နိုင်သည်။ လက်ရှိ version တွင် wallet၊ payment၊ transfer နှင့် wagering function မပါဝင်ပါ။</p></div><section className="golden-panel mt-10 max-w-3xl p-6 sm:p-8">{loading ? <QueryLoading /> : !user ? <div className="text-center"><LockKeyhole className="mx-auto h-8 w-8 text-lime-200" /><h2 className="mt-4 text-2xl font-black text-white">Manus login အရင်ဝင်ပါ</h2><p className="mt-3 text-sm leading-6 text-emerald-50/65">Agent ထံမှရရှိသော invitation link ကိုဖွင့်ပြီး login ဝင်ပါ။</p><Button onClick={() => startLogin()} className="mt-6 bg-lime-200 text-[#09221c] hover:bg-lime-100">Manus login ဝင်ရန်<ArrowRight className="ml-2 h-4 w-4" /></Button></div> : user.role === "agent" ? <div className="text-center"><UserRound className="mx-auto h-8 w-8 text-lime-200" /><h2 className="mt-4 text-2xl font-black text-white">ဤ account သည် Agent ဖြစ်နေသည်</h2><p className="mt-3 text-sm leading-6 text-emerald-50/65">Agent နှင့် Player identity ကိုခွဲထားသည်။</p></div> : user.role === "admin" ? <div className="text-center"><UserRound className="mx-auto h-8 w-8 text-lime-200" /><h2 className="mt-4 text-2xl font-black text-white">Admin account သည် Player account မဟုတ်ပါ</h2><p className="mt-3 text-sm leading-6 text-emerald-50/65">Player အတွက် Agent invitation link နှင့် plain Manus account ကိုအသုံးပြုပါ။</p></div> : profile.isLoading ? <QueryLoading /> : profile.isError ? <QueryError label={humanizeError(profile.error)} onRetry={() => profile.refetch()} /> : !profile.data ? <div className="text-center"><Link2 className="mx-auto h-8 w-8 text-lime-200" /><h2 className="mt-4 text-2xl font-black text-white">Agent invitation link လိုအပ်သည်</h2><p className="mt-3 text-sm leading-6 text-emerald-50/65">Player profile ကို public page ကနေတန်းဖွင့်လို့မရပါ။ Agent ထံမှရရှိသော Player link ကိုဖွင့်ပြီး account ဖွင့်ပါ။</p></div> : <div><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-lime-200">ACTIVE PROFILE</p><h2 className="mt-2 text-2xl font-black text-white">{profile.data.displayName || user.name || "SKY1688 Player"}</h2><p className="mt-2 text-sm text-emerald-50/60">Manus login identity verified · Profile status: active</p></div><CircleCheck className="h-10 w-10 text-lime-200" /></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><a href="/results" className="group border border-white/10 bg-white/[.03] p-5"><p className="text-sm font-bold text-white">2D / 3D result ကြည့်ရန်</p><p className="mt-2 text-sm leading-6 text-emerald-50/55">Admin publish ပြုလုပ်ထားသော result များကိုဖတ်ပါ။</p><span className="mt-4 inline-flex items-center text-sm font-bold text-lime-200">Result သို့သွားရန် <ArrowRight className="ml-1 h-4 w-4" /></span></a><a href="/dreams" className="group border border-white/10 bg-white/[.03] p-5"><p className="text-sm font-bold text-white">Dream1000 ကြည့်ရန်</p><p className="mt-2 text-sm leading-6 text-emerald-50/55">Published dream content များကိုရှာဖွေဖတ်ရှုပါ။</p><span className="mt-4 inline-flex items-center text-sm font-bold text-lime-200">Dream1000 သို့သွားရန် <ArrowRight className="ml-1 h-4 w-4" /></span></a></div></div>}</section></div></PublicShell>;
}
